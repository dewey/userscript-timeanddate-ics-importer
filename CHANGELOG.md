# Changelog

## 0.0.1

- Initial release
- Calendar discovery via `/cal/` and import via `/ev/`
- Birthday calendars detected via `anniversary: true`
- ICS parsing via `ical.js`
- Optional use of calendar color for imported events
- Settings persisted in `localStorage`
